# Project Log - 2026-05-09 - Hub Rename Fixes

## Task
Fix compilation errors in `pkg/hub/` resulting from the Project rename (Grove -> Project).

## Changes
- Modified `pkg/hub/handlers.go`:
    - Updated `enrichAgents` to use `agents[i].Project` instead of `agents[i].Grove`.
    - Updated `enrichAgent` to use `agent.Project` instead of `agent.Grove`.
    - Replaced string literal `"grove"` with `"project"` where appropriate (scopes, resource types).
    - Replaced `"grove:"` prefix with `"project:"` for group slugs.
    - Added backward compatibility for `"grove"` scope in `resolveEnvSecretAccess`.
- Modified `pkg/hub/handlers_gcp_identity.go`:
    - Renamed `GCPQuotaGroveInfo` to `GCPQuotaProjectInfo`.
    - Updated `GCPQuotaResponse` and `GCPMintQuotaInfo` fields.
    - Updated JSON tags for better consistency with the rename.
- Modified `pkg/hub/handlers_notifications.go`:
    - Added backward compatibility for `"grove"` scope in notification subscription listing.
- Modified `pkg/hub/authz.go`:
    - Renamed resource type and parent type `"grove"` to `"project"`.
    - Updated bypass logic comments and function names.
- Modified multiple test files in `pkg/hub/` to match the rename:
    - Updated string expectations.
    - Updated struct field names in test environments (`integrationTestEnv`, `notificationTestEnv`).
    - Fixed corrupted group slugs (e.g., `project:gcp-authz-project:members` -> `project:gcp-authz-grove:members`).
- Modified `pkg/hub/server.go`:
    - Fixed route collisions between `projects` and legacy `groves` aliases.
    - Ensured `/api/v1/groves/` routes are correctly aliased to project handlers.
- Modified `pkg/hub/agenttoken.go`:
    - Updated `ScopeGCPTokenPrefix` to use `project:` prefix.
- Removed `pkg/hub/grove_webdav_test.go` (duplicate of `pkg/hub/project_webdav_test.go`).
- Fixed an unused variable `settings` in `pkg/config/shared_dirs.go`.

## Verification Results
- `go build ./...` passes.
- `go test -run=^$ ./pkg/hub/...` passes (tests compile).
- Verified `agent.Project` and `agents[i].Project` usages in `pkg/hub/handlers.go`.
